## C to Minecraft 编译器架构设计文档（定稿）

### 项目概述
本项目是一个将 C 语言子集编译为 Minecraft Java Edition 1.20.5+ 数据包的编译器。前端基于 libclang 获取抽象语法树，后端采用 C++ 实现代码生成。目标平台利用 `/function` 宏机制实现动态栈帧寻址，以计分板（scoreboard）和存储（storage）作为运行时数据载体。

### 设计原则
- **不扩展 C 语法**：所有功能均基于标准 C 语法实现，特殊行为通过注释约定或编译器内部识别处理。
- **零成本信任原则**：运行时错误（除零、数组越界等）遵循 C 标准的未定义行为，不做额外检查，由 Minecraft 的静默失败承载。
- **平台适配优先**：充分利用 Minecraft 1.20.5+ 的宏、计分板和 storage 特性，将平台限制转化为设计约束，实现高效、简洁的编译后端。
- **渐进式交付**：分版本迭代，每个版本均可独立测试和交付，降低开发风险。

### 技术栈
- 前端：libclang（C/C++ binding）提供 AST 解析
- 后端：C++，利用 `nlohmann/json` 库处理 JSON 生成
- 目标平台：Minecraft Java Edition 1.20.5+
- 核心机制：计分板（scoreboard）、存储（storage）、宏函数（`/function ... with`）

---

## 版本演进计划

### V1.0 — 纯整数运算与函数调用（预计 500-800 行核心 C++）
**状态**：✅ 已达成（2026-08），游戏内实测通过；符号表输出已完成（JSON 注释嵌入各 mcfunction）。
**核心目标**：跑通从 C 源码到可执行数据包的完整流程，验证内存模型和宏栈帧机制。

#### 特性
- **数据类型**：仅 `int`，映射为计分板虚拟玩家分值。
- **表达式**：整数四则运算、取模、括号、单目负号。
- **变量管理**：
  - 全局变量：固定命名 `$global_xxx`
  - 局部变量：`$fn$(stack)_xxx`，通过宏替换展开，实现基于栈帧的动态寻址。
- **函数调用与递归**：
  - 全局栈指针 `stack_ptr`，调用时递增，返回时递减。
  - 使用 `/function ... with {stack: $(stack_ptr)}` 传递栈帧号。
  - 全局计分板 `return` 传递返回值。
  - 支持递归调用，每次调用分配独立栈帧。
  - ⚠️ 实现说明：递归机制已随 `functionSpace` 深度计数就绪，但**尚未实测**（V1.0 无控制流，无法写出跳出的递归用例），留待 V2.0 引入 `if` 后验证。
- **临时变量**：函数内计数器法分配，命名 `$fn$(stack)_tmpN`，遵循 LIFO 自动释放。
- **语句**：表达式语句、`return` 语句、复合语句（顺序执行）。
- **基础调试支持**：
  - 编译时输出符号对应表（变量名、函数名与计分板玩家名的映射关系）。
  - 内建函数 `print_int`，利用 `storage` 中转和宏参数，在聊天栏打印整数。

#### 不包含
控制流、数组、指针、浮点、字符串、世界交互。

---

### V2.0 — 控制流、受限指针与混合构建（预计 +800 行核心 C++）
**核心目标**：支持结构化控制流、受限指针、变长数组，并实现与手写 mcfunction 的互操作。

#### 特性
- **控制流**：
  - `if`/`else`：编译为独立函数，用 `execute if/unless score` 实现条件跳转。
  - `while`/`for`：编译为尾递归函数，在函数末尾根据条件决定是否继续递归。
  - 所有控制流子块接收相同的 `stack` 宏参数，共享父函数的局部变量。
- **受限指针**：
  - 兼容标准 C 的 `*` 和 `&` 语法，不引入新关键字。
  - 指针变量底层为 `int`，存储目标变量的编号。
  - 支持赋值、取地址、解引用；禁止算术运算和强制类型转换（编译时报错）。
  - 野指针视为未定义行为，不进行运行时检查。
- **变长数组**：
  - 支持定长和变长数组语法，底层处理完全统一。
  - 数组声明中的长度采用“提示性语义”（君子协议），编译器不分配空间、不检查边界。
  - 元素访问 `arr[i]` 通过宏展开为 `$fn$(stack)_base_i` 动态寻址，越界由 Minecraft 静默处理。
- **混合构建与互操作**：
  - 通过命名空间隔离（如 `compiled:` 与 `custom:`）避免冲突。
  - 定义公共参数槽位（如 `$param0`, `$param1`, `$return_val`）作为调用约定。
  - C 代码调用手写函数时，编译器自动插入参数拷贝指令；反之亦然。
- **循环展开优化**（V2.5 可选）：
  - 特殊格式注释 `// @max_iter N` 提示最大循环次数。
  - 编译器识别后对小次数循环进行完全展开，消除递归调用。

#### 不包含
浮点、字符串、世界交互内建函数。

---

### V3.0 — 浮点数与字符串（预计 +600 行核心 C++ 及运行时库）
**核心目标**：扩展类型系统，支持浮点运算和字符串处理。

#### 特性
- **浮点数**：
  - 软件模拟，每个浮点数占用两个计分板槽位（有效数字、指数）。
  - 加减乘除通过运行时辅助函数（对齐、移位、舍入）实现。
  - 打印时结合除法和取模分别输出整数和小数部分。
- **字符串**：
  - **可计算字符串**（`char[]`）：每个字符映射为一个计分板槽位，支持下标读写。
  - **常量字符串**：直接存入 `storage`，支持全 Unicode，适合提示语等场景。
  - 提供字符串打印、拼接、长度等运行时辅助函数。

#### 不包含
世界交互、数据包元数据。

---

### V4.0 — 世界交互内建函数（预计 +300 行核心 C++）
**核心目标**：提供操作 Minecraft 世界的内建函数库及自定义命令执行能力。

#### 特性
- **内建世界操作**：`mc_fill`、`mc_setblock`、`mc_summon`、`mc_give` 等，分别对应不同命令形式。
- **自定义指令函数 `mc_exec`**：可变参数，接受编译期常量字符串，直接拼接为任意命令执行（作为未封装命令的逃生舱）。
- **调试日志**：利用已有的 `print_int` 和字符串系统，提供更丰富的调试输出。
- **头文件策略**：提供 `mcbuiltin.h` 供 IDE 识别，内部使用 `#ifndef __MC_COMPILER__` 条件编译，编译器定义该宏后跳过声明，直接硬编码识别。

#### 不包含
合成表、成就等数据包元数据生成。

---

### V5.0 — 数据包元数据生成（预计 +300 行核心 C++）
**核心目标**：编译生成合成表、成就、标签、战利品表等数据包 JSON 文件。

#### 特性
- 提供特殊函数，如 `__mc_recipe`、`__mc_advancement` 等。
- 用户通过 C 结构体传递数据，编译器在编译期提取常量，利用 `nlohmann/json` 序列化为 JSON 文件。
- 类型安全、IDE 友好，所有数据与程序逻辑统一存放在 C 源码中。

---

### V6.0 — 源代码级调试器（预计 +500 行核心 C++）
**核心目标**：提供“语句即函数”的调试模式，支持断点和变量查看。

#### 特性
- 在调试模式（`--debug`）下，每个 C 语句编译为独立的 `.mcfunction` 函数。
- 调试调度器根据 `next_stmt` 计分板决定执行哪个语句函数。
- 实现手动标记断点（`__breakpoint()`）和当前栈帧变量转储。
- 利用 Minecraft 原生函数调用栈实现调用栈回溯。
- 发布模式保留原有内联生成，性能不受影响。

---

## 功能清单汇总
| 版本 | 核心特性 |
|------|----------|
| V1.0 | 整数运算、函数调用、递归（待验证）、宏栈帧、基础调试（符号表+print_int） |
| V2.0 | 控制流（if/while/for）、受限指针（无算术）、变长数组（提示性长度）、混合构建 |
| V2.5 | 循环展开优化（`// @max_iter` 注释） |
| V3.0 | 软件浮点、可计算字符串、常量字符串 |
| V4.0 | 世界交互内建函数、`mc_exec` 自定义指令、调试日志 |
| V5.0 | 合成表、成就等数据包元数据生成 |
| V6.0 | 源代码级调试器（语句函数、断点、变量查看） |

---

## 结语
本编译器架构经过从临时变量分配、表达式求值策略到完整六阶段演进的深入推演，充分利用了 Minecraft 1.20.5+ 的宏、计分板和 storage 机制，实现了高度自洽的 C 语言到数据包的编译方案。设计上遵循不扩展 C 语法、信任目标平台、零成本抽象等原则，将平台限制转化为创新契机。项目采用渐进式交付，每个版本独立可测，降低了单人开发的难度与风险。至此，架构设计已基本完整，可以进入 V1.0 的实现阶段。